<Window x:Class="SSYM.OrgDsn.UI.View.Support.About"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="درباره نرم افزار"
        Width="400"
        Height="400"
        MinWidth="400"
        MinHeight="400"
        MaxWidth="400"
        MaxHeight="400"
        Background="{DynamicResource aboutBGImage}">

    <Grid FlowDirection="RightToLeft">
        <StackPanel VerticalAlignment="Bottom"
                    Margin="25,0,15,98">
            <TextBlock Text="گروه نرم افزاری SSYM"
                       Foreground="{DynamicResource appClr1}" />
            <TextBlock Text="نرم‏افزار معماری سازمان "
                       Margin="30,0"
                       Foreground="{DynamicResource appClr1}" />

            <StackPanel  Margin="50,0" Orientation="Horizontal">
                <TextBlock Text="نسخه"
                           Foreground="{DynamicResource appClr1}" />

                <TextBlock Name="tbkVer"
                           Text=" 0.1.1.0"
                           FontFamily="Arial"
                           Foreground="{DynamicResource appClr1}" />
            </StackPanel>

        </StackPanel>
    </Grid>

</Window>
